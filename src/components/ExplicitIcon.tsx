import { Card, Tooltip } from "@mui/material";

const ExplicitIcon = () => {
  return (
    <Tooltip title="Explicit">
      <Card sx={{ p: 0.5 }}>E</Card>
    </Tooltip>
  );
};

export default ExplicitIcon;
